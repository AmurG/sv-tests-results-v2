# sv-tests results — CIRCT clean-dense, v2 (aggressive: receipted test-content overlay)

Full [sv-tests](https://github.com/chipsalliance/sv-tests) dashboard (all standard tool columns, stock report generator, no page edits) with the two end-to-end columns recomputed in full at the shipped-engine tip of 2026-08-25 — a direct continuation of the prior page's build line, on the SAME published arm as the prior page, carrying the STANDING (2026-08-23) correction of record about which arm that is. The 2026-08-19 and 2026-08-22 revisions of this page called the four-state honest power-on arm *the engine's DEFAULT*. It is not. The four-valued lowering is opt-in — `Default false` in the engine's own option table — this board reaches it through a wrapper that adds the flag, and a default invocation of the compiler, and of the shipped simulator, is TWO-VALUED. The `Arcilator` column below is therefore the OPT-IN arm; the arm a user gets by default is published in full as `Arcilator_extended` on the [v3 page](https://amurg.github.io/sv-tests-results-v3/) at **4664/5092**. The arm bullet below carries the whole correction. THE HEADLINE OF THIS PUBLISH: an engine step of 12 named opt-in gains — the power/arithmetic unknown-operand value-semantics family and the rnmos/rpmos/cmos switch-level primitive class — plus ONE named HARNESS repair (`fx68k`, the ledger bullet below) that flips on every arm, defaults included; green-to-red is ZERO on every column of this publish, and the prior publish's grader-honesty disclosures are STANDING (their bullets below) — under the campaign's receipted test-content overlay (test-bug repairs, each with a named LRM clause — the laneb MANIFEST series plus the two lanee `report_phase` NBA repairs plus patches 13, 14, 15 and 04c2): `circt_verilog` (the DEFAULT two-valued invocation) **4999/5093** (**uvm-tagged cell 169/169 all-green**), `Arcilator` (the OPT-IN four-valued arm — reached by passing a flag, NOT by a default invocation) **4877/5092 — BOARD ≡ HONEST** (**uvm 162/169**, unchanged — still down two on the reopened `property/sequence_local_var_test_uvm` pair, named below; the case-leak scanner reads ZERO on this board's own logs; same truncation-FIXED test driver, same engine line, SAME arm as the prior page — the step vs the prior page's 4864 is **13 red-to-green and ZERO green-to-red, every gain named**: 12 engine cures — the power/arithmetic and switch-level primitive families, the ledger bullet below — plus the `fx68k` HARNESS repair, not booked as engine movement — and BOARD and HONEST stay the same number because the fixed grader still reads a case-leak of zero here; the honest ladder since the driver correction reads 4545 → 4566 → 4578 → 4621 → 4610 → 4637 → 4798 → 4829 → 4865 → 4864 → **4877**), `Slang_circt` (the fork frontend at the Moore boundary; carried unchanged for five generations — its own build is still the 2026-08-02 snapshot — the cross-column invariant vs this page's fresh `circt_verilog` column re-measured at this publish: 42 rows, decomposed and CORRECTED in the carry bullet below, dominated by the timing-annotation compat class the carried build predates; provenance below) **4972/5089**, and `Slang` (our stock-slang run of 2026-07-18 on this overlay + the adopted content, carried forward — provenance below) **5088/5089** — real `uvm_pkg` (`ARCILATOR_UVM_NO_STUB=1 CIRCT_UVM_SHIMS=1 CIRCT_UVM_FABRICATED_REPORTS=0`, `OVERRIDE_TEST_TIMEOUTS=900` on the Arcilator column, a disclosed harness parameter — applied as a FLOOR of each row's own declared budget as of this publish, no longer a clip (the SoC bullet below; zero rows moved on this account); `--ignore-timing-annotations` on both CIRCT columns' frontend invocations — a documented compatibility flag with a mandatory loud notice, the timing-annotation bullet below). Slang itself is stock v11 — the aggressive case is entirely test-content repair. CIRCT bins: built from the campaign's engine tree of 2026-08-25. **PUBLICATION POSTURE (standing, by ruling): the dashboards are public; the engine source is NOT published.** Results provenance — the exact source tree, build receipts and board banks — is retained privately, every number on this page is reproducible from that retained record, and nothing on this page implies the source is available (each row's `circt_ref` reads `not published`). Two qualifications stated plainly. (1) **Lineage**: this build IS a descendant of the prior page's build — the same integrated line, one further stamped integration wave ahead of it. The earlier pages added *and none behind*; that is withdrawn as stated, because at least one cure proven on a side branch is not an ancestor of this tip (checked by merge-base, not assumed). The line is forward-only with respect to every PUBLISHED build; it is not a superset of every fix the campaign has ever proven. The `circt_verilog` comparison below is therefore a single-line progress ledger with zero green-to-red; the `Arcilator` comparison is a single-line ledger PLUS STANDING GRADER CORRECTIONS — the case-leak fix and the prior publish's verdict-scanner repair (its bullet below) re-graded rows the old scanners over-credited; at THIS publish green-to-red is ZERO on every column. What is NOT retired, stated plainly: the 2026-08-02 line is still a separate line, and most of the fixes it carries that this line does not are still absent — the changes list names which of them came back at this publish and which are still red. (Build-lineage claims here describe the privately retained provenance record.) Per-feature narrative bullets of the 2026-08-02 page describe that page's build and are preserved in the pages' revision history, not silently re-asserted here; every disclosure kept on THIS page is re-measured on this build's boards. (2) **Build fidelity**: the privately retained provenance tree is, bit for bit, the exact source the boards' binaries were built from (tree-hash equality verified at staging). Both CIRCT-backed columns were measured in full at this tree — 5093 `circt_verilog` rows and 5092 `Arcilator` rows, nothing spliced or carried from an earlier build. Neither the source tree nor the development history is published. The CIRCT-backed runs use the harness-comparability policies 26-32 (26-31 unchanged: nonexistent include dirs dropped; the stock Slang runner's ibex accommodation mirrored; declared-top-only top selection — no top guessing on any of our columns; uvm-scoped `--allow-use-before-declare` on all CIRCT-backed columns; POLICY 32, introduced at the 2026-07-14 publish, is disclosed output/scanner hygiene — `-o /dev/null` on the `circt_verilog` full-pipeline invocation keeps the emitted IR text out of the measured log; the invocation was already full-pipeline and grading criteria are unchanged; see the changes list and the grading note below).

**Changes at this publish (2026-08-25 second revision, vs the earlier 2026-08-25 page).**

- **Publication posture (standing since the 2026-08-19 publish): the dashboards are public; the engine source is NOT published.** The per-row provenance fields read `not published`, the tool-header links point at this results page itself, and results provenance — the exact source tree, build receipts and board banks — is retained privately; every number on this page is reproducible from that retained record. Prior change ledgers are preserved in the pages' revision history; standing disclosures below are carried where re-measured on this build. **Reading order note:** this page and the prior (earlier 2026-08-25) page are measured on the same driver, the same suite state and the SAME published arm; the aggregate deltas below are one stamped integration wave's engine step (12 named gains, ZERO green-to-red) plus one HARNESS repair (`fx68k`, the ledger bullet below; the harness timeout override additionally became a FLOOR of each row's declared budget — zero rows moved on that account); the standing driver-correction bullet below remains the required context for comparisons against any page published BEFORE 2026-07-28.

- **THE CASE-LEAK GRADER DEFECT IS FIXED (standing since the 2026-08-22 publish) — BOARD ≡ HONEST, re-verified on this publish's four boards, where the scanner reads a case-leak of zero on every one.** Since 2026-07-28 this page has published two lenses: BOARD (the rig's own `report.csv` `Pass` column) and HONEST (BOARD minus the case-leak rows — rows scored green whose runtime output holds only a title-case `Failed` diagnostic and neither `PASSED` nor upper-case `FAILED`; the old strict scanner matched upper case only, so those greens hung on the case of one letter). The defect was in the GRADER, not the engine, and it is cured at the runner: a printed self-reported failure verdict now demotes the row unconditionally at scoring time, so `report.csv` itself carries the honest verdict. Measured consequences on this board: (a) the case-leak scanner reads **ZERO** on this publish's boards — the two lenses are numerically identical and the dual-lens split is RETIRED; (b) exactly the **21** rows the prior page's HONEST lens deducted flip green-to-red on this column — the board number gives back precisely what the old scanner over-credited (`regress-sv_br_gh460_iv`, `regress-vlg_array_select_a_iv`, `regress-vlg_ca_force_iv`, `regress-vlg_powca_iv`, `regress-vlg_powproc_iv`, `regress-vlg_pr2202706b_iv`, `regress-vlg_pr2728032_iv`, `regress-vlg_pr2801662_iv`, `regress-vlg_pr2824189_iv`, `regress-vlg_pr2971207_iv`, `regress-vlg_pr3054101c_iv`, `regress-vlg_pr3054101d_iv`, `regress-vlg_pr3054101e_iv`, `regress-vlg_pr3054101f_iv`, `regress-vlg_pr3054101g_iv`, `regress-vlg_pr3054101h_iv`, `regress-vlg_real_assign_deassign_iv`, `regress-vlg_real_force_rel_iv`, `regress-vlg_real_reg_force_rel_iv`, `regress-vlg_undef_lval_select2_iv`, `regress-vlg_undef_lval_select_iv`); the engine's behaviour on those rows is unchanged (re-grading the prior tip's own banked logs under the fixed grader flips exactly the same set — an adjacent A/B, receipted in the campaign records); (c) the honest page-to-page comparison is therefore prior HONEST 4798 → **4829**, and the prior BOARD 4819 is the number the grader correction re-grades. (Two of the 21 — `regress-vlg_powca_iv`, `regress-vlg_powproc_iv` — are ENGINE-CURED at this publish with their own passing self-checks; the ledger bullet below.)

- **TWO MORE VERDICT-SCANNER HOLES CLOSED AT THE EARLIER 2026-08-25 PUBLISH (STANDING, re-verified here) — the give-back was 11 named rows on this column, zero engine regressions among them.** The strict evaluator that grades these boards had two blind spots, found by this wave's audits and repaired in the GRADER, not the engine: (a) a bare all-caps `FAIL` verdict token was invisible to the verdict scan — 21 ivtest sources print exactly that (`$display("FAIL")`), so a row could print `FAIL` and stay green (fixed word-bounded, upper-case; zero-count summaries and PASS/FAIL prose excluded); (b) the runtime-output scan applied only to `.sv` sources, so a `.v`-source simulation that produced NO output at all (rc=0, empty payload) kept its green — absence of output is now a failure condition there too. The repair was validated with an old-vs-new battery BEFORE any board ran: the old grader reproduces all six banked prior arms exactly, the new grader's flip set equals an independently surveyed prediction exactly, and doctored control logs behave (a planted bare-`FAIL` bites, a planted silenced row bites, the negative control is unchanged). Give-back on this column, all named: `regress-sv_test_tliteral_iv`, `regress-vlg_br_gh621_iv`, `regress-vlg_br_ml20150315_iv`, `regress-vlg_div16_iv`, `regress-vlg_integer5_iv`, `regress-vlg_mult16_iv`, `regress-vlg_pr1687193_iv`, `regress-vlg_pr2029336_iv`, `regress-vlg_pr2883958_iv`, `regress-vlg_sp2_iv` (the ten silenced-simulation rows — every one also flips on the DEFAULT arm: grader honesty is arm-independent) plus `regress-vlg_pr1676071_iv` (a bare-`FAIL` printer, four-valued arm only — its default-arm run does not print the token and stays green). The engine's behaviour on all eleven is unchanged by the repair, and all eleven stay red at this publish.

- **REOPENED AT THE EARLIER 2026-08-25 PUBLISH (STANDING — both stay red here), honestly: `property_local_var_test_uvm` and `sequence_local_var_test_uvm`.** Both were green on the 2026-08-23 page and are red since, and the cause is disclosed rather than smoothed over: their prior green was MASKED by a change that violated IEEE 1800-2023 4.5's region ordering (readers scheduled to observe mid-timestep values), and that change is EXCLUDED from this build for exactly that violation — a cure obtained through a region-order-violating change is not a cure. Their state here is byte-consistent with the pre-mask record (the same 6 property-layer firings), and the proper cure path (moving the NBA commit point to the LRM's NBA region) is tracked. The `uvm` cell's -2 vs the 2026-08-23 page is exactly this pair (the cell is unchanged at this publish).

- **TIMING-ANNOTATION COMPAT HATCH ON (documented flag, loud; STANDING since the 2026-08-22 publish, re-measured here).** `--ignore-timing-annotations` now rides both CIRCT columns' frontend invocations (disclosed in the invocation line of every row log). The engine refuses specify-path / timing-check / `$sdf_annotate` constructs by default with loud, named refusals; the documented compatibility flag DROPS the annotations instead — every dropped site gets its own warning AND the run gets a mandatory once-per-run summary (`ignore-timing-annotations: dropped N timing annotation(s) in this run; the design simulates with zero path delay everywhere those annotations applied`), present in **42** of this board's `Arcilator` row logs and **37** of its `circt_verilog` row logs. Row effect when it landed at the 2026-08-22 publish: **+29** on this column (28 of the 41-row July refusal ledger + `regress-vlg_real_delay_iv`, an SDF row outside it whose log carries the dropping-`$sdf_annotate` warnings) and **+37** on `circt_verilog` (36 of the ledger + the same SDF row) — 13 ledger rows stay red on this column. HONESTY CAVEAT, stated plainly: a green under this flag is a **zero-path-delay** simulation of the annotated design; rows whose own self-checks observe the dropped delays still fail honestly (six observe the dropped delays in their self-checks — `regress-vlg_br1006_iv`, `regress-vlg_br_gh316a/b/c_iv`, `regress-vlg_br_gh356a/b_iv` — plus the five bufif1-family wall rows and `regress-vlg_specify2_iv` + `regress-vlg_pr2829776b_iv`), and about a dozen of the recovered rows carry external golds whose event timing depends on those delays — their greens are rc/self-check greens under zero delay, not timing-accurate reproductions. Do not read the class as timing support; it is a documented compatibility posture with the loss printed into every affected log.

- **Assertion pass-action vacuity — a documented, reference-aligned deviation (STANDING since the 2026-08-22 publish; zero verdict effect at this one).** The SVA verdict path now reports implication properties PER OBLIGATION at their own completion ticks with per-attempt dedup (validated event-for-event against the campaign's banked external references on the receipted assertion battery), and pass actions execute on NONVACUOUS successes only. IEEE 1800-2023 20.11's default sentence reads the other way (vacuous successes execute the pass action unless `$assertvacuousoff`), while 16.14.8 states a vacuous success "is not considered a pass or a failure"; every external reference the campaign has banked for this construct suppresses vacuous pass actions at its defaults (measured on their recorded runs, not assumed), and the engine follows that measured consensus. The 20.11 sentence is recorded as a DOCUMENTED DEVIATION (restorable via the `$assertcontrol` machinery, tracked). Row effect on this page: **zero** (no verdict on these boards moves on this account); disclosed because assertion-heavy testbenches that COUNT pass-action executions will observe nonvacuous-only counts.

- **The overlay set is UNCHANGED at this publish** (canonical 12-patch set + 13 + 14 + 15 + 04c2, sha-receipted identical to the 2026-07-14 and earlier 2026-07-17 pages; all 20 overlay files byte-verified again at this run); the deltas below are entirely engine-line (the overlay did not move). The v1 board is strictly stock (standing since the 2026-08-23 publish, when the fixture `do_print` repair stopped riding it — its adoption note).
- `circt_verilog` 4998 → **4999** (net **+1** — the single mover is `fx68k`, the named HARNESS repair (the ledger bullet below), not an engine change: this wave's ENGINE work moves the compile tool's DEFAULT invocation by zero rows, the point of the invisibility bullet above; the 169-row `uvm` cell reads 169/169). The OPT-IN four-valued compile arm moves **5028 → 5038** (published on the [v3 page](https://amurg.github.io/sv-tests-results-v3/) as `circt_verilog_extended_4state`): `fx68k` plus the 9-row switch-level primitive class — this wave's frontend-import feature work necessarily moves the elaboration-only compile arm, while the DEFAULT compile invocation keeps all 9 rows red symmetrically. Two of the 9 are compile-arm-only gains that stay red on the simulate column, each with a named cause: `regress-vlg_br_gh99u_iv` now SIMULATES and prints its own failing self-check (an honest engine red, tracked), and `regress-vlg_test_mos_strength_reduction_iv` is a GRADER-LENS red — its simulation prints only PASSED, but an engine diagnostic in the same log quotes IR text containing `FAILED:`-shaped string literals that the whole-log verdict scan trips on (the same textual class as the `cva6_*` reds; a location-scoped diagnostic fix is tracked). The `regress-vlg_scanf4_iv` lineage gap stays red and stays named (re-measured at this tip). `Slang_circt` and `Slang` are carried (fourth generation — the carry decision bullet below).
- `Arcilator` — **BOARD ≡ HONEST 4877/5092** (the dual lens was retired at the 2026-08-22 publish and stays retired: BOARD = the rig's own `report.csv` `Pass` column, what this page's table and per-tag cells show; the fixed grader reads a case-leak of zero on this board). Same driver, same engine line, same arm as the prior page: vs its 4864 the flip ledger is **13 red-to-green and ZERO green-to-red, every gain named**. Twelve are engine cures, attributed by the PRIOR board's own failure text, not by assumption: **5 are the power/arithmetic unknown-operand value-semantics family** (`regress-vlg_arithunknown_iv`, `regress-vlg_powca_iv`, `regress-vlg_powproc_iv`, `regress-vlg_pr2909386a_iv`, `regress-vlg_pr2909386b_iv` — each printed its own failing self-check verdict on the prior board and passes it now; the cures are the zero-to-a-negative-power and unknown-operand arithmetic rules of IEEE 1800-2023 Table 11-4 and kin, plus a subtraction-fold guard; two of the five, `powca`/`powproc`, are 2026-08-22 case-leak give-back rows honestly re-earned), **7 are the switch-level primitive class** (`regress-sv_br_gh1223c_iv`, `regress-vlg_br_gh99k_iv`, `regress-vlg_cmos_iv`, `regress-vlg_rnpmos_iv`, `regress-vlg_rnpmos2_iv`, `regress-vlg_test_rnmos_iv`, `regress-vlg_test_rpmos_iv` — the rnmos/rpmos/cmos/rcmos resistive and complementary MOS primitives land this wave; each row's prior log refused the primitive at import — `error: unsupported primitive` — and each now compiles and simulates to its own passing verdict), and **1 is `fx68k` — a HARNESS repair, not an engine gain**: the prior boards' invocation failed at elaboration (`'fx68kTop' is not a valid top-level module`) because the harness omitted the compile-time define (`FX68K_TEST`) that gates this core's testbench top, and it additionally dropped a duplicate-initial-driver compatibility flag the engine implements; the harness now passes both, the row flips on ALL FOUR arms — both defaults included — and this page does not book it as engine movement. The prior (earlier 2026-08-25) publish's 18 named gains all stay green, and its 19 named give-backs all stay red with their causes standing: the 11 verdict-scanner rows (its bullet above), the two `bp_multicore_4*` giants and the two `cva6_*` grader-lens reds (the SoC bullet above), the reopened `property/sequence_local_var_test_uvm` pair (its bullet above), `regress-vlg_br_gh315_iv` (an honest engine red, named and tracked), and `regress-vlg_pr1662508_iv` (a bare-`FAIL` printer under time-0 x-to-0 negedge evaluation; external-reference adjudication owed on an LRM-ambiguous ordering). Net, the column moves 4864 → **4877**; the ENGINE-ATTRIBUTABLE step is **+12**. The rig's two ledgers diverge by exactly 1 row on this arm (report 4877 vs strict 4878) — the grading-lens bullet below. The 169-row `uvm` cell is **162/169**, unchanged at this publish — still down two on the reopened `property/sequence_local_var_test_uvm` pair (its bullet above). Denominator note: **5092** — `regress-vlg_pr587_iv` is harness-excluded on this column (standing; the row is present on the `circt_verilog` column, denominator 5093). `uvm_tb_build_ss_tb` stays green on this board.
- **The 2026-08-06 lineage rows, re-measured on this page's OPT-IN arm (verdicts unchanged at this publish).** Of the 16 rows the 2026-08-06 page named as its lineage cost, **4 are green on this page's `Arcilator` column**: `regress-vlg_pr2425055a_iv`, `regress-vlg_sdw_always3_iv`, `regress-sv_always_comb_void_func_iv`, `regress-vlg_array_lval_select6_iv`. The 12 red: `regress-sv_always_comb_warn_iv`, `regress-sv_always_ff_warn_iv`, `regress-sv_always_latch_warn_iv`, `regress-sv_array_size_iv`, `regress-sv_sv_ps_array_cassign_iv`, `regress-vlg_array4_iv`, `regress-vlg_delay5_iv`, `regress-vlg_ldelay1_iv`, `regress-vlg_ldelay3_iv`, `regress-vlg_pr1696137_iv`, `regress-vlg_task_mem_iv`, `regress-vlg_pr1570451_iv`. Of those, 1 (`regress-vlg_pr1570451_iv`) is green on this same build's DEFAULT (two-valued) arm and red here — it is a member of the named arm-cost list above, not a lineage residue.
- **The SoC block on the opt-in arm — 12 of 16 green at this publish; all sixteen stay green on the DEFAULT arm.** Green on this page's `Arcilator` column: `TNoC`, `bp_default`, `bp_unicore`, `bp_multicore_1`, `bp_multicore_1_cce_ucode`, `rsd`, `scr1`, `veer-eh1`, `veer-el2`, `cva6_cv64a6_imafdc_sv39`, `cva6_cv64a6_imafdch_sv39`, `cva6_tb_cv64a6_imafdc_sv39`. The four red here are named with their causes. The two `bp_multicore_4*` giants are rc=71 timeouts AT their own declared 1000 s budgets (1002.5 s and 1002.6 s measured; a RIG CORRECTION at this publish: the harness's timeout override now acts as a FLOOR — max(declared, 900) — and no longer clips these rows' declared 1000 s budgets to 900 s): on this build the four-valued arm retains register state it previously dropped on these cores — a receipted correctness repair on this line — and the retained state costs wall-clock; an engine-side fix this wave removed a quadratic diagnostic-printing hot spot in the state-lowering pass on this family (383× on the largest configuration, output-certified byte-identical), and the rows remain simulation-bound past their budgets even so. The prior page already declined to claim their greens as an engine gain (it published them as contention); this page publishes their reds as the price of keeping the registers, with the budget stated. On the DEFAULT arm the same two rows run in 159.8 s and 213.9 s and stay green. The two `cva6_*` rows (`cv32a6_imac_sv32`, `cv64a6_imafdc_sv39_hpdcache`) are rc=0 GRADER-LENS reds — the crash heuristic matching a struct field name inside this wave's four-valued-aggregate warnings, disclosed in the ledger bullet above; elaboration completes on both. Read this together with the BlackParrot `--parse-only` disclosure below and do not merge the two: the `--parse-only` accommodation applies to the `circt_verilog` and `Slang_circt` columns, which remain parse evidence for those rows, while the `Arcilator` column never used it and is a compile-and-simulate result.
- **WHICH ARM EACH COLUMN MEASURES — CORRECTION OF RECORD, STANDING SINCE THE 2026-08-23 PUBLISH, re-measured here.** The 2026-08-19 and 2026-08-22 revisions of this page said *"four-state honest power-on initialization is the engine's DEFAULT"*. **That was wrong, and it is corrected here.** The word *default* was carrying two different meanings and only one of them was true. (1) **The campaign's default board of record** — a reporting convention: the arm the campaign chooses to headline. That is the arm this page's `Arcilator` column measures, and it is reached by **passing a flag**: the board's binaries for this arm are a two-line wrapper that prepends `--four-valued` to the same engine binary the other arm runs raw (both resolve to one sha; the test harness itself never passes the flag). (2) **What a user gets by default** — two-valued. In the engine's own option table the four-valued lowering reads `Default false = the unchanged two-valued path (four-valued IntType -> bare iN, x/z discarded)`, and a default invocation lowers an uninitialized 4-state variable to a concrete zero: it prints `0000`, not `xxxx`. The shipped command-line simulator does not carry the flag string at all; reaching this arm there requires `--four-state`. **So: this page's `Arcilator` column is the OPT-IN four-valued arm. The arm a default invocation gets is published in full as `Arcilator_extended` on the [v3 page](https://amurg.github.io/sv-tests-results-v3/) — 4664/5092 at this same tip (uvm cell 166/169).** What W26 actually made default-ON is a narrower thing: honest x/z *power-on* within four-valued lowering, which the engine's own header records as applying *only where four-valued leaves exist ... the two-valued arm has no such leaf types and is UNCHANGED BY CONSTRUCTION*. The previously published claim that `CIRCT_HONEST_XINIT=0` *restores the two-state arm* is therefore also withdrawn: that switch does not reach the two-state arm, because the two-state arm is what a default invocation already is. Three consequences, RE-MEASURED on this build: (a) the same-tip arm-cost set (rows green on the DEFAULT arm and red on this opt-in arm) is **22 rows** — the carried members `assert0_test_uvm`, `assert_final_test_uvm`, `regress-sv_sv_string6_iv`, `regress-vlg_br930_iv`, `regress-vlg_br_gh127b_iv`, `regress-vlg_br_gh127e_iv`, `regress-vlg_fscanf_u_iv`, `regress-vlg_fscanf_z_iv`, `regress-vlg_pr1570451_iv`, `regress-vlg_pr2172606b_iv`, `regress-vlg_pr2728032_iv`, `regress-vlg_sscanf_u_iv`, `sva_sva_value_change_sim` (two prior members — `regress-sv_edge_iv`, `regress-vlg_memport_bs_iv` — LEFT the set at the earlier 2026-08-25 publish, cured on the opt-in arm) plus the 9 rows the earlier 2026-08-25 publish's ledger added (all still members): the two `bp_multicore_4*` giants, the two `cva6_*` grader-lens reds, `regress-vlg_br_gh315_iv`, `regress-vlg_pr1662508_iv`, `regress-vlg_pr1676071_iv`, and the reopened `property/sequence_local_var_test_uvm` pair — each named with its cause in the standing bullets above, each failing LOUDLY, none silently wrong (measured: none of the 22 exits 0 with a clean transcript — the rc=0 members carry the very diagnostic or `FAIL` text that demoted them); the two `assert*_test_uvm` rows are the four-valued arm's answer where an `x`-valued expression is false at the assertion statement and the two-valued arm's concrete zero makes it true, and the earlier description of them as a *genuine assertion misfire* is withdrawn as unsupported in that direction — their adjudication is tracked; (b) at this tip the opt-in arm is green-only on **235 rows** against **22 rows** red-only, **+213 net** — the value-fidelity families the arm exists for (struct/union and packed-aggregate x propagation, x/z literals and wildcard compares, readmem/meminit x frames, net strength/tristate); read that net as a verdict-lens number, not a fidelity score, and read the x-honesty bullet below for the direction it points; (c) HISTORICAL-SERIES CAVEAT (carried, unchanged): before the 2026-08-18 wave the four-valued arm carried a defect that committed every register on a process-driven clock ONE EVALUATION LATE (the end-of-eval clock-reload defect — every plain testbench clock generator was affected); it is cured on this line, but any comparison against four-state-arm numbers published before that cure is NOT like-for-like.
- **SAID ONCE, PLAINLY: EVERY FOUR-VALUED-GATED CURE ON THIS PAGE IS INVISIBLE TO A DEFAULT INVOCATION.** The conformance work this campaign has been landing on the four-valued arm — out-of-range and x/z select semantics, signed out-of-bounds, four-valued sub-reference sourcing, the switch-level primitives and drive strengths of the 2026-08-23 publish, the packed-vector write-through-select value semantics of the earlier 2026-08-25 publish, and at this publish the rnmos/rpmos/cmos switch-primitive class and the power/arithmetic unknown-operand semantics — reaches a user only if that user opts in. Measured, not asserted: this publish's ENGINE work lands 12 gains on the `Arcilator` column and moves the DEFAULT arm's engine verdicts by **0 rows** — the default arm's only movement (4663 → **4664**) is `fx68k`, the named HARNESS repair, which flips on every arm (harness honesty is arm-independent); the compile column's default arm moves by exactly the same single harness row (4998 → **4999**) and is otherwise an exact row-for-row match. Of the 12 engine rows this page gains, **all 12 are still red on the default simulate arm at this very tip**, and not one of the 12 changed its default-arm verdict. The same split runs the other way through the product: the compatibility flags this board relies on — the four-valued lowering and the timing-annotation hatch — are passed by the measurement harness and are **not** carried by the shipped command-line simulator, so a green here is a statement about the engine reached with those flags, not about a default run of the shipped tool. Being in the tree is not the same as being in the product, and this page does not claim otherwise.
- **Log-artefact disclosure (standing, first shipped 2026-08-19): row logs carry additional slang diagnostic lines vs the pre-2026-08-19 pages.** That page's engine interval included a slang frontend bump; measured on the campaign's own page-against-page lens, roughly 15-20% of row logs gain diagnostic lines, and the dominant shape is a byte-identical REPEAT of a diagnostic the row already emitted at the same file:line:column — duplication, not new findings. Verdict impact: ZERO (no row's verdict moves on this account), and the disappearance direction was checked: no passing row quietly dropped a diagnostic. Those lines remain in these logs; THIS publish's interval adds no frontend bump — its own log-shape changes are the value-content changes of the 12 named engine gains (the power/arithmetic family now prints its self-check passes; the switch-primitive rows now simulate at all) and the changed `fx68k` harness invocation; the earlier 2026-08-25 publish's scanner demotions and four-valued-aggregate warnings remain in the affected logs. Anyone diffing this page's logs against the prior page's should be told this rather than discover it.
- **Grading-lens divergence (disclosed; BOARD stays the report lens per this page's standing definition).** The stock renderer's log parser previously evaluated assertion-shaped oracle text found anywhere in the log body, including inside diagnostic context; at this rig (since the 2026-08-23 publish) that evaluation is SCOPED to non-diagnostic context (a grader fix, validated against the strict ledger), which recovered `sequence_stable_test_uvm` on the page lens at that publish — its red was a renderer artifact, strict-green since the 2026-08-17 wave; the row stays green here. The two ledgers now disagree by exactly **1 row** on this arm: report 4877 vs strict 4878, the row `basic-tagged-union` (report-red/strict-green, chapter 7.3.2, not uvm-tagged — the uvm cell reads 162/169 on BOTH ledgers). The page publishes the LOWER, as-rendered number for every cell rather than patching its own renderer; the strict count is stated here.
- **Test-suite state (standing): `tests/` is byte-identical to upstream chipsalliance/sv-tests @ `c4229f3b` — unchanged at this publish** (no upstream window adopted; the overlay gate re-verified all 20 overlay files byte-identical at this run). The 2026-08-19 page's +4 sequence-quartet scoring note is that revision's record, preserved in this repo's history.
- **Randomize honesty — the 18.6.9 ordered trade (standing, first shipped on the 2026-07-30 page; the mechanism is on this line's ancestry and its zero-cost/behavior statements are re-measured on this page's boards):** `randomize()` calls whose constraint set this implementation cannot honor now **return 0 loudly** (a compile warning plus a one-shot runtime decline note naming the undrawable shape) instead of the legacy accepted-form return-1-with-nothing-drawn; `rand` dynamic arrays/queues are genuinely drawn, `unique` constraints enforced, and the scope `std::randomize`/register-model bound shapes solve honestly. This is a deliberate honesty trade: a design whose own `assert(randomize())` leaned on the silent accepted-form green now fails loudly on the return-value channel the test itself checks. On THIS page's row set the trade cost **zero rows** (0 green-to-red on all four boards). A fidelity gap is retained for disclosure: `pre_randomize` is not invoked on declined calls.
- **`$monitor`/`$fmonitor` Postponed-region fidelity (standing, first shipped on the 2026-07-30 page; the mechanism is on this line's ancestry and its zero-cost/behavior statements are re-measured on this page's boards):** monitor output is now deferred to the Postponed region instead of firing per delta pass (21.2.3), matching the line ordering that clause specifies — the spurious extra time-0 line is gone. No row verdict on this page moved on this change alone.
- **Tristate net-driver resolution (standing, first shipped on the 2026-07-30 page; the mechanism is on this line's ancestry and its zero-cost/behavior statements are re-measured on this page's boards):** conditional (tristate) net drivers now resolve against their co-drivers before the z-folded value is destroyed, curing a last-write-wins silent-wrong class on the wired-net family (pullup, wand/wor, tri01/supply — receipts in the campaign records). Rows whose green depended on the old last-write-wins path would flip toward the resolved co-driver value; on this page's row set **none did** (0 green-to-red measured on all four boards).
- **Control-plane and format fidelity (standing, first shipped on the 2026-07-30 page; the mechanism is on this line's ancestry and its zero-cost/behavior statements are re-measured on this page's boards):** `%t` and `$timeformat` are wired through the runtime formatter (the `time5` green above), time-to-int conversions round to the nearest unit, `$test$plusargs`/`$value$plusargs` follow 21.6 (int result, string-expression forms) with plusargs kept in command-line order, model stdout is ordered against the transcript (and the transcript flushes before a runtime diagnostic reaches stderr), and display-list null arguments print the LRM single space (21.2.1.2).
- **Test-driver correction (standing context, first shipped 2026-07-28; required for any comparison against pages published BEFORE 2026-07-28):** the pre-2026-07-28 generations' generated-C++ test-driver emitter TRUNCATED long driver sources at an internal buffer boundary, so affected rows compiled, exited rc=0 with their self-checks never executed, and graded green for no reason. The emitter was FIXED at the 2026-07-28 publish; re-grading the 2026-07-26 page's own binaries with the fixed driver gave the truncation-fixed baseline **4545/5091** (83 of that page's arc greens were truncation-inflated — none uvm-tagged; 64 of the 83 have no banked AnonSim gold and are externally unconfirmed in either direction). The honest lineage is therefore the fixed-driver ladder above, never a comparison against the retracted 4630-era figures.
- **The SVA-sequence family on the opt-in arm — the standing four-row disclosure is SUPERSEDED by measurement.** The prior pages shipped `sequence_changed/fell/past/rose_test_uvm` red with a compiler cause (no Reactive-region visibility). The Reactive-region NBA cure landed on this line (2026-08-17 wave), and on THIS build all four are **strict-green on the opt-in four-valued arm** (4/4 by the strict ledger) — the engine cause is CURED, on both arms — AND green on the page lens as well (since the 2026-08-19 publish: upstream's suite update removed the expectation marker the report lens was mis-scoring). `sequence_stable_test_uvm` joined the family on the page lens at the 2026-08-23 publish (the renderer parseLog scoping fix — the grading-lens bullet above) and stays green here: the published sequence family is green on this column, both ledgers. Two uvm-tagged deferred-assert rows DO ship engine-red on this arm: `assert0_test_uvm`, `assert_final_test_uvm` — green on the DEFAULT (two-valued) arm and red here (`Error: assert failed` at run phase). The earlier description of this pair as a *genuine assertion misfire* is WITHDRAWN: on the opt-in arm the asserted expression is `x` at the assertion statement and an `x` is not true, while on the two-valued arm the same expression is a concrete zero; which of those is the conforming answer is tracked as an open adjudication, not settled here, and the rows are published red as measured. See the arm bullet above.
- **The standing named-red register vs the truncation-fixed baseline, re-measured on the opt-in arm:** `regress-vlg_sdw_force_iv` stays CURED (green here); the four `sequence_*` rows are engine-cured and report-lens-red (their own bullet above); `hdlconvertor_std2017_p612` (the standing GRADER false positive: a correct diagnostic containing the word *aborted* trips a `process_crash` heuristic; counted red because that is what the board scored) remains red on both arms. The green-to-red rows vs the PRIOR PAGE are the arm move, disclosed in the arm bullet above.
- **SVA-sequence runtime fingerprint (line-specific; NOT carried):** the 2026-08-02 page's remediation statement (0.47-0.53x on the five bisected rows) described that page's build and line and is preserved in the pages' revision history; it is a per-line timing claim, this page's line has not been fingerprint-measured, and no timing claim rides this page.
- **`Slang_circt` and `Slang` stay CARRIED (decision recorded at this publish):** `Slang_circt`'s own build is still the 2026-08-02 snapshot (fifth carried generation) and `Slang` is still the 2026-07-18 run. Rerunning either is a full column run of its own and was deliberately NOT done at this publish; the carry is stated here rather than implied, and the rerun is now NAMED AS OWED — the carried generation has drifted measurably behind the fresh frontend (next sentence). The `Slang_circt` ⊇ `circt_verilog` prefix invariant was re-measured against THIS page's fresh `circt_verilog` column on this page's own CSV: **42 rows** are cv-green/`Slang_circt`-red, decomposed: **32** are this publish's timing-annotation compatibility class (the fresh column runs with the documented compat flag; the carried 2026-08-02 build predates the flag and still refuses those constructs loudly — generation skew, not a frontend regression), **1** is `ibex` (the static-initializer cure of this interval, likewise newer than the carried build), **2** are the standing named exceptions (`hdlconvertor_std2017_p718` lint-mode; `regress-vlg_contrib83_iv` `should_fail` black-box), and **7** are pre-existing rows (`hdlconvertor_std2017_p372`, `hdlconvertor_std2017_p751`, `hdlconvertor_std2017_p752`, `hdlconvertor_std2017_p757`, `hdlconvertor_std2017_p779`, `regress-sv_slongint_test_iv`, `regress-sv_ulongint_test_iv`) that the prior two revisions' "exactly two named exceptions, zero new" sentence MISSED — **CORRECTION OF RECORD: that sentence was wrong on the prior pages' own published CSVs (9 rows measured there, re-checked on the live data during this staging), and the claim is replaced by this measured decomposition.** The invariant remains a COMPILE-lens statement, untouched by the simulation arm.
- **`tagged_union_member_access_inv` (standing structural disclosure, first shipped 2026-07-26 — `circt_verilog` red on both boards, `Arcilator` green):** this chapter-11.9 `should_fail: 1` row expects a runtime violation from reading the wrong tagged-union member. The frontend now accepts tagged-union member access (stock slang accepts this code too — the row is compile-clean), and the new runtime tag check correctly traps the invalid access in simulation: the `Arcilator` (simulate) column observes the trap and passes, while the compile-graded `circt_verilog` column can no longer observe any failure and ships an honest red. The earlier pages' `circt_verilog` green was a non-support artifact (the row passed compile-grading only because tagged-union member access was rejected altogether). Disclosed rather than forced: the row is red as measured on the compile-graded column, and it is the single named exception to the zero-`Arcilator`-green/`circt_verilog`-red invariant (clarification bullet below).
- **Dynamic part-select writes inside loops (standing semantics disclosure, first shipped 2026-07-26):** the 2026-07-26 cycle's part-select legalization lowers dynamic part-select writes (`x[base +: w] = v`) through a read-modify-write in straight-line procedural contexts only, with a 1024-bit base-vector width guard (both restrictions are receipted regression fixes: the unguarded forms produced an elaboration blow-up on part-select-rich RTL and a pathological code-gen shape on 32768-bit UVM pack buffers). Dynamic part-select writes inside loops, and on wider bases, keep the pre-existing wrapped lowering path and its semantics. A canonicalizer-livelock reproducer for the general looped form is banked in the campaign records; an upstream canonicalizer fix would lift the restriction.
- **`hdlconvertor_std2017_p718` (standing documented cross-column exception, first shipped 2026-07-26 — `Slang_circt` red, `circt_verilog`/`Arcilator` green):** the import pipeline now supports `interconnect` nets (6.6.8), the source of this row's new greens. The `Slang_circt` column has always run a 342-row multi-file subset (this row included; identical row set on the prior pages) in slang's lint-compilation mode, and that mode still rejects references to interconnect nets outside net port connections — behavior receipted UNCHANGED across the last three binary generations. The row is therefore a named single-row exception to the `Slang_circt ⊇ circt_verilog` prefix invariant (provenance section below): a standing lint-mode limitation newly exposed by the import-side gain, not a frontend regression.
- **`uvm_tb_build_bl_tb` FIXED at the 2026-07-24 publish (standing disclosure, carried; both boards' `Arcilator` columns; the row stays green at this publish):** the 2026-07-23 pages shipped this row as a disclosed honest-red task-livelock. The root cause is now found, and it **supersedes the prior page's cumulative-composition description**: a failed blocking-retry poll probe in spawned sequence-start frames re-allocates a by-ref temp box on every pass; the fresh pointer lands in the coroutine's state record, so the scheduler pump's record-changed heuristic read every failed probe as progress and re-woke every heap-parked waiter — a self-sustaining zero-delay wake storm (two such probers wake each other forever). Whether a given binary livelocked was decided by allocator/heap-layout lottery, which is why the earlier bisect found no owning commit. The fix is a per-task streak cap: past 64 consecutive non-firing poll re-steps at one timestamp, that task's record-change bump is suppressed as probe noise (fail-loud note; genuine wakes unaffected). A six-layout link sweep goes 6/6 livelock before the fix to 6/6 deterministic pass after it, and the row completes with natural end on both boards since the 2026-07-24 publish (receipts in the campaign lane records, laneovernight24 t1straggler + the BC18 board cycle). The row stays green as measured, not re-rolled.
- **The 7 uvm-tagged `Arcilator` reds on the opt-in arm, each named with an owner (the DEFAULT arm carries 3 of the 7 — the deferred-assert pair and the reopened local-var pair are green there):** `assert0_test_uvm`, `assert_final_test_uvm` (the four-valued deferred-assert pair — the arm's named open item), `property_local_var_test_uvm`, `sequence_local_var_test_uvm` (REOPENED at this publish — the adjudicated exclusion cost, their own bullet above; owner: the NBA-commit-point cure, tracked), `uvm_id_register` (the standing pinned-tuple ceiling correction below), `uart_example` (the frontend refusal of its four-valued sub-reference sourcing is CURED this interval — the row now compiles clean and is green on the `circt_verilog` column; it stays red HERE as a LOUD rc=71 timeout against the 900s budget — a named simulation throughput wall, tracked), and `uvm_c_stimulus_bl_example` (now also compile-clean and cv-green; red here as a LOUD run-phase abort, rc=134 at 57 s — tracked; the prior page's frontend-rejection description of these two rows is superseded by measurement). The five `sequence_*` rows are OFF this list — the full published sequence family is green on this column at this publish (their bullet above). `uvm_tb_build_bl_tb` stays off (fixed 2026-07-24); `uvm_tb_build_ss_tb` stays green.
- **Harness/tuple adoptions (standing since the 2026-07-18 page; disclosed; receipts in the campaign lane records; tuple unchanged ON THIS PAGE at this publish — the v1 page narrows its tuple, see its adoption note):** four small, receipted changes to the measurement tuple ride this page — (1) a **uvm-library compatibility shim**: 22 lines of legacy UVM-1.x sequence-library utility macro aliases appended to the (deprecated) `uvm_sequence_defines.svh` of the pinned UVM library, letting 1.x-era tests elaborate against the modern library; (2) a **fixture `do_print` repair** on the `uvm_use_models_pipelined_item_done` mbus sequence item (IEEE 1800.2-compatible printer API, single-file overlay) — at this publish carried on THIS page's overlay content only; the v1 page is strictly git-clean stock and no longer applies it (its row goes honestly red there, disclosed on the v1 page); (3) a **runner uvm-tag predicate fix** (rows tagged `uvm-1.2`/`uvm-*` are now treated as UVM rows by the simulation harness — the previous exact-token test ended genuine UVM simulations at t=2ns); and (4) a **generated-harness symbol sanitization** (4-site struct-name mangle in the C++ driver generator, fixing name collisions in the emitted model header). All four are content/harness-level and tool-neutral; the runner baseline carrying (3)+(4) is sha-pinned in the campaign records and was gate-checked before every board. The `uvm_use_models_pipelined_item_done` and `uvm_virt_seq_find_all` greens on this page (both `uvm-1.2`-tagged, outside the 169-row `uvm` cell) continue to exist because of adoptions (1)-(3); the stock-`Slang` column was RERUN on the same adopted content (provenance below) so the zero-violation cross-column invariants remain measured, not carried.
- **`regress-vlg_pr1077_iv` (honesty caveat, pre-registered):** this row counts green under the rc/marker grader at this publish, but its output does NOT yet match the ivtest (Icarus) gold expectation (a 4-state + clock-liveness expectation) — it is carried as **wall-advanced, not as an honest behavioral pass**; at the 2026-07-28 publish the honest new-green count on the `Arcilator` column was therefore 62 (+1 caveat row); the row is unchanged (still green, still gold-mismatched) at this publish. Tracked in the 4-state family.
- **`regress-vlg_triand_iv` (standing honesty exposure, first disclosed on the earlier 2026-07-17 page; still red here):** the pre-2026-07-17 pages' green was hollow — the event queue went quiescent at step 2, BEFORE the test's self-checks ran; at this tip the simulation executes the checks and honestly exposes unimplemented multi-driver `triand` wired-AND resolution (`FAILED a=0,b=1,net=1`). Tracked as a named wall (wired-net resolution family), (the earlier pages' standing `regress-vlg_memidx_iv` scheduler-wake residue is GREEN on this line as measured).
- **`$monitor`/`$fmonitor` re-land — the earlier page's `fmonitor_task` caution is LIFTED:** the value-change-gated `$monitor`/`$fmonitor` lowering that was reverted at the previous integration gate (its module-global last-message writes re-fired per settle pass under the new combinational conversion) re-landed this cycle with the IEEE 1800 §21.2.3 gating **purity-gated** on the process body, removing the receipted interaction. `monitor_task` and `fmonitor_task` both grade green on both boards' `Arcilator` columns (the ~5.5s full-run timing was measured on the prior line and is not re-asserted) — no settle-spin, no quiescent-at-step-2 signature; these cells are now stable feature evidence. (The previous cycle's LowerProcesses signal-resample repair remains in place, disclosed on the earlier page.)
- **4-state semantics — the "opt-in four-state / two-state default" caveat is BACK ON, corrected.** The 2026-08-19 and 2026-08-22 revisions retired this caveat on the strength of the arm claim the arm bullet above withdraws. The accurate statement is the original one: **four-state value semantics are opt-in and two-state is the default**, in the engine and in the shipped simulator alike. This page's `Arcilator` column publishes the opt-in arm, so the X-propagation compile-time ladder and the dynamic-X debt notes of the earlier pages — which described the two-state arm — still apply in full to a default invocation, and that arm is published as `Arcilator_extended` on the [v3 page](https://amurg.github.io/sv-tests-results-v3/). The `regress-vlg_pr1077_iv` caveat (green under the rc/marker grader, gold-mismatched) is re-measured unchanged and stays a caveat row.
- **POLICY 32 (output/scanner hygiene, disclosed; prose corrected 2026-07-15):** `-o /dev/null` was added to the `circt_verilog` invocation so the emitted IR text is no longer echoed into the measured log. The invocation already ran the full compile pipeline before this change; pipeline depth and grading criteria are unchanged. What it cures: cva6 hpdcache struct field names in the printed IR (`aborted: i1`) falsely tripped the strict-eval scanner (`\bAborted\b` → process_crash), demoting two compiler-green rows. Row effect: exactly the 2 `cva6_*` rows above. It also makes the page's native per-tag cells and the CSV record agree exactly for `circt_verilog` (grading note below). An earlier revision of this note described POLICY 32 as a move from elaboration-only to full-pipeline grading — that was incorrect as written.
- **Per-tag summary-matrix fix (defect disclosure):** on the previous pages the index summary matrix's `circt_verilog` per-tag cells were stale — reading up to 279 rows lower than the page's own `report.csv` (a favorable-direction error: the page understated our own column; headline totals and the CSV record were unaffected). This publish fully re-renders the summary matrix from the fresh logs for ALL tools, and a new mandatory publish gate asserts every tool x tag cell equals the CSV-derived count on both dashboards (verified pre-push and again on the live pages).
- **`uvm_id_register` ceiling correction (second-reference parity receipt; wording corrected 2026-07-15):** the second reference implementation fails this row with the identical zero-read signature our `Arcilator` log shows (`[ID_IDX_WR] Index N returned 0 expected 160+N`, 26 UVM_ERRORs, `!! UVM TEST FAILED !!`) — time-exact identical **logged error events** (the same timestamped error signature) for the exact pinned test/UVM/fixture tuple. The parity claim is limited to those logged error events; no equivalence beyond them is claimed. Source inspection additionally proves deterministic zero readback for this exact tuple (an 8-bit `ID` field on a 32-bit `UVM_BIG_ENDIAN` map: the pinned UVM library byte-reverses the 64-bit access while the 32-bit APB adapter/DUT use the low lane untranslated) — a pinned fixture/library lane-contract defect, not a universal claim about every simulator/UVM configuration. The earlier revisions projected an honest ceiling of 168/169 from this row; that projection is withdrawn, because it treated the two deferred-assert rows as recoverable and the arm bullet above no longer asserts that. What this page publishes instead is what it measured: **162/169** on the opt-in arm and **166/169** on the default arm, with the difference named row for row above. **Re-verified at this publish, broader than the banked receipt:** `uvm_id_register` — red by example defect, proven by reference identity: our transcript is line-for-line identical to the second reference's, including every timestamp and all 26 UVM_ERRORs. The example's `create_map(..., UVM_BIG_ENDIAN)` makes `uvm_reg_map` byte-swap the full 64-bit `uvm_reg_data_t`, destroying the 8-bit payload in both directions inside the UVM library itself — no conforming IEEE 1800.2 simulator can pass this row as graded.
- **`moore` / `moore_parse` clarification:** these two columns are stock upstream reference tools reproduced from the chipsalliance baseline — they are NOT this fork's frontend. `Arcilator`-total exceeding `moore`-total is expected and is not an inconsistency; the enforced cross-column invariant is ARM-MATCHED: zero `Arcilator`-green/`circt_verilog`-red rows outside named structural disclosures when both columns run the SAME arm — verified at this publish on the arm-matched extended columns of the [v3 page](https://amurg.github.io/sv-tests-results-v3/): exactly one named exception on BOTH arm-matched pairs, `tagged_union_member_access_inv` (the runtime-observable `should_fail` disclosure above). CORRECTION OF RECORD, measured on this page's own CSV: since the 2026-08-23 arm relabeling this page's `circt_verilog` column is the DEFAULT arm while `Arcilator` is the OPT-IN arm, so the same comparison taken ACROSS arms reads **41 rows** here — the named exception plus 40 rows, every one of the 40 a four-valued-gated family member (switch-level primitives, drive strengths and kin; each is green on the OPT-IN compile arm, measured) that the opt-in simulate arm passes and a default compile invocation refuses: the invisibility bullet above, measured a second way (34 at the earlier 2026-08-25 publish; the +7 is exactly this publish's switch-primitive gains, whose default-compile refusals persist). The 2026-08-23 page's revision of this bullet asserted 'exactly one named exception' after the relabeling; on its own published CSVs the cross-arm count was already 34, and the correction (2026-08-25, standing) is re-measured here.
- **Scheduler-boundary budget rows (footnote update):** `uvm_register_spi_bl_tb` remains boundary-watched (green this cycle on both boards; disclosed as measured, never re-rolled); `uvm_register_mem_example` stays retired from the boundary list (genuinely green). `uvm_tb_build_bl_tb` stays OFF the named-regression register (fixed at the 2026-07-24 publish — the poll-probe wake-storm disclosure above; green on both boards this cycle; the fix's claim remains the layout-deterministic 6/6 link-layout sweep, not a re-rolled lottery draw).
- **`Arcilator` x-honesty reclassification (standing, shipped 2026-07-13; adjudicated below) — RE-MEASURED, WITH A CORRECTION OF RECORD:** the 96 rows listed in [`fold96-arc-reclassification.tsv`](fold96-arc-reclassification.tsv) were surfaced 2026-07-13 as honest reds (the case-equality constant-fold change exposed `=== 'x`-style self-checks passing vacuously on the then-2-valued engine). Measured at this publish: **86 of the 96 are green on this page's `Arcilator` column — the opt-in arm** (`regress-sv_always_ff_iv` joined the greens at the earlier 2026-08-25 publish) — the fold's honest reds were dominated by the genuine four-state gap, which the OPT-IN four-valued arm implements — and the split is **exactly inverted on the arm a user gets by default**, where 11 of the 96 are green and 85 are red. That inversion is the single clearest measurement of what opting in buys and of what a default invocation does not get — and **10 stay red**: `regress-sv_always_comb_no_sens_iv`, `regress-sv_br_gh164b_iv`, `regress-sv_br_gh337_iv`, `regress-sv_sv_darray_oob_vec4_iv`, `regress-sv_sv_queue_oob_vec4_iv`, `regress-vlg_br_gh99g_iv`, `regress-vlg_pr1570451b_iv`, `regress-vlg_pr2121536_iv`, `regress-vlg_pr2121536b_iv`, `regress-vlg_time4_iv`. **CORRECTION OF RECORD:** the prior two revisions of this bullet carried the two-state-arm count ("11 of the 96 genuinely green / 85 honest reds") across the 2026-08-19 arm flip unchanged; on those pages' own published CSVs the `Arcilator` column already read 85 green / 11 red — an understatement of the page's own column, corrected here. The reference-relative ADJUDICATION of 2026-07-13 (82 genuine 4-state gaps / 10 ceiling corrections — the tests' own selfchecks fail on the second reference too / 4 reference-inconclusive parse gaps) is that adjudication's record and stands unchanged; its classes are per-row in the TSV. Sweep disclosure carried: the reference runs were compiled with the UVM library's `UVM_NO_DPI` macro defined, disabling its DPI layer (irrelevant to this plain-SV family).

**Overlay state, per column.** ALL FOUR of our columns ran on the same overlay: the canonical 12-patch set (including the two lanee `report_phase` NBA repairs) plus four receipted repairs — patch 13 (`apb_protocol_monitor`: instance-dot `get_coverage()`, IEEE 1800 §19.8-legal, semantics-identical; the stock text dots the covergroup TYPE, which stock slang rightly rejects), patch 14 (`uvm_interrupts_*` trio: parentheses on the non-void `get_status()` call, IEEE 1800-2023 §13.5/Syntax 13-3 fn 42; stock-slang-master + `circt_verilog` treatment receipts), patch 15 (`uart_example` `*_seq_item` trio: UVM-1.1-era printer-knob/`sprint` API brought to the modern UVM API; receipted against an independent front end (control and treatment, both forms accepted); standing, carried unchanged), and patch 04c2 (`uvm_tb_build_ss_tb`: packages-first reorder of this page's generated descriptor, IEEE 1800 §26.3; verdict-neutral; standing, carried unchanged — supersedes the earlier 04c note, which was cut against a different descriptor generation and was not applied). The rendered test sources on this page show the applied overlay text. The 16 baseline columns are STOCK-test results: the overlay was not rerun on the third-party tools; for the all-stock comparison see [sv-tests-results-v1](https://amurg.github.io/sv-tests-results-v1/); the four-state arm is the OPT-IN arm this page headlines, not a default (the arm bullet above); for the DEFAULT two-valued arm (`Arcilator_extended`) and both arms' extended columns see [sv-tests-results-v3](https://amurg.github.io/sv-tests-results-v3/).

**Reference implementations (terminology).** Two independent SystemVerilog implementations are used on these pages as external references. The first is called `AnonSim` throughout; the second is called the second reference. Neither is one of the 20 tool columns published on this page, and neither is this fork. Every claim on this page that rests on an external reference states which of the two produced it, and no claim on this page is adjudicated by both.

**Column provenance.** Every other column (16 columns) reproduces the verified hybrid baseline — the upstream chipsalliance sv-tests run `e4fc087` ([actions run 28414450742](https://github.com/chipsalliance/sv-tests/actions/runs/28414450742), dated 2026-06-30). Raw third-party logs are not retained locally, so this page was regenerated by the stock sv-tests generator over minimal stub logs synthesized from the verified baseline `report.csv`; regeneration reproduced every baseline row's Pass/ExitCode and every per-tool pass total exactly (verified row-by-row), the remaining metadata fields are restored byte-exact from the baseline record in `report.csv`, and each stub log page states its own provenance. The `moore` and `moore_parse` columns in that baseline set are upstream reference tools, unrelated to this fork's columns. The `circt_verilog`, `Arcilator`, `Slang` and `Slang_circt` columns carry real, full logs from fresh runs (logs >150KB middle-truncated with an explicit marker; grading used the full logs).

**`Slang` column provenance.** The `Slang` column is OUR local run of 2026-07-18 on this page's applied overlay text (20/20 overlay files sha-verified at this publish) INCLUDING the two disclosed content adoptions, carried forward for this publish (binary, overlay and adopted content unchanged since that run; the superset invariant vs the fresh `circt_verilog` column re-measured at this publish: zero violations) (the uvm-library shim + the `do_print` fixture repair; same binary as the prior carried run — flip audit vs the prior page's Slang column: exactly the 2 adoption rows red-to-green, zero green-to-red; the superset invariant vs the fresh `circt_verilog` column re-measured: zero violations): stock slang master `11.0.0+b60d729` ([MikePopoloski/slang @ `b60d729d66b`](https://github.com/MikePopoloski/slang/tree/b60d729d66b9cdeec158b800f898461a138d505e), the binary actually used, receipted at run time), run on this page's own (overlay-patched) test trees with the matched invocation (POLICY 28/29): `slang-driver --single-unit --timescale=1ns/1ns -I <test incdirs> -Wno-error=index-oob -Wno-error=range-oob -Wno-error=range-width-oob -Wno-implicit-conv`, declared-top-only `--top`, and `--allow-use-before-declare` on uvm-tagged rows; every log page shows its exact command line. Page cells and `report.csv` agree exactly (native stock-generator grading; zero strict-grade changes). Flip audit vs the previous page's Slang column: `uart_example` red-to-green only, zero green-to-red.

**`Slang_circt` column provenance.** This column is ours, not part of the upstream chipsalliance baseline. History: it was first added on 2026-07-02 (`add_slang_circt_column_20260702.py`) as a standalone `slang-driver` run built from slang `9.0.99+12898c34d` — the slang pin our CIRCT toolchain embedded at that date (the per-row `runner_url` on the rendered log pages pointed at slang `f357dec…`, which was a runner metadata artifact — the submodule pin, not the binary; the run's `version` record proves the `12898c34d` binary). As of the 2026-07-08 publish the column is **redefined at the 2026-07-08 publish, last RERUN at the 2026-08-02 publish, and CARRIED here (one generation; the invariant vs this page's fresh columns re-measured, two named exceptions)**, as the CIRCT fork's own frontend surface at the Moore boundary: the `circt-verilog` binary of its own run generation (the 2026-08-02 engine build (source not published); embedded slang v11.0.0 per the run's version record; this page's fresh `circt_verilog` column is the current build above), invoked with `--parse-only` (slang parse + elaborate + Verilog import, no MooreToCore/LLHD lowering) and byte-identical frontend flags on its parse-mode rows (a standing 342-row multi-file subset runs in slang's lint-compilation mode — the row-mode manifest this column has always carried; identical row set on the prior pages). It therefore satisfies `Slang_circt ⊇ circt_verilog ⊇ Arcilator` row-for-row by pipeline-prefix construction on the parse-mode rows (re-measured at this publish and CORRECTED (the prefix-invariant sentence of the earlier revisions was wrong on those pages' own CSVs): **42 rows** are `circt_verilog`-green and `Slang_circt`-red, and **16 rows** run the other way; the carried column's own build predates the timing-annotation compat class that dominates the first list, and its rerun is named as owed, both disclosed above). Note this is a **definition change**: the previous standalone-slang number (5073) measured pure slang elaboration; the current column also includes the importer stage, so its total is lower and is not comparable to the 5073. As of the 2026-07-08 suppression-strip change the importer carries **no source-keyed diagnostic suppressions**: every slang error surfaces unmodified, and the only frontend relaxations are explicit stock-slang-equivalent flags (`--allow-use-before-declare` on uvm-tagged rows — POLICY 31, matching the invocation the `Arcilator` and `Slang` columns always carried — `--ignore-unknown-modules`, ...) shown on each row's command line. The prefix-superset property is re-measured same-generation on THIS page and the earlier two-exception sentence is WITHDRAWN: **42 rows** are `circt_verilog`-green and `Slang_circt`-red and **16** run the other way, decomposed in the carry bullet above and gated by name.

**Documented cross-column exceptions.**

- `circt_verilog` pass / stock-`Slang` fail: **zero rows.** The importer's former legacy-UVM diagnostic suppressions were removed at the 2026-07-08 suppression-strip change; the previously listed exception rows now fail `circt_verilog` with the same diagnostics stock slang reports and are tracked as named climb targets, not exceptions.
- `apb_protocol_monitor` is reclassified as a **test bug**: the stock text dots a covergroup TYPE name through an interface instance (`APB.APB_accesses_cg.get_coverage()`), which IEEE 1800-2017/2023 §19.8 reserves for `::` type access; stock slang rejects it with the same diagnostic (`cannot use dot operator on a type name`). No conforming frontend can pass the stock row. This page carries the receipted §19.8-legal repair (patch 13), under which stock slang also passes — and at this tip the row is green end-to-end on `Arcilator` with genuine covergroup coverage.
- `Slang_circt ⊇ circt_verilog ⊇ Arcilator`: measured same-generation at this publish; the exceptions are: `tagged_union_member_access_inv` (`Arcilator`-green/`circt_verilog`-red: the standing runtime-observable `should_fail`), `hdlconvertor_std2017_p718` (`circt_verilog`-green/`Slang_circt`-red: the standing lint-mode row), and `regress-vlg_contrib83_iv` (`circt_verilog`-green/`Slang_circt`-red: the attributed `should_fail` black-box disclosure above). Zero violations outside these named lists (the 5-row Arcilator-pass/`circt_verilog`-fail set published before 2026-07-09 was cured by POLICY 29 declared-top-only selection — the old cells failed on a guessed `--top=top` that rejected sibling-top hierarchical references).
- `Slang_parse` (baseline column) is parse-only: `should_fail` negatives that parse cleanly and are only rejectable at elaboration count against it by definition, so its inversions vs the elaborating columns are definitional, not engine gaps.
- 4 unsupported-mode rows are absent from the `Slang` and `Slang_circt` row sets (`11.9--tagged_union_member_access_inv`, `16.10--property-local-var-fail`, `16.10--sequence-local-var-fail`, `16.15--property-disable-iff-fail`) — the same 5089-row denominator as the upstream Slang column.

- **BlackParrot `--parse-only` rows (runner-defect disclosure, 2026-07-15):** the six `black-parrot` rows on the `circt_verilog` and `Slang_circt` columns of this page were run with `--parse-only`. Four (`bp_multicore*`) are intentionally parse-only (invalid top-level parameter values / configurations that `$fatal` at elaboration); `bp_default` and `bp_unicore` received the flag unintentionally through an always-truthy runner predicate (a bare `'bp_uce'` string literal). An exact full-pipeline A/B replay (only `--parse-only` removed; argv otherwise byte-identical) keeps both rows green on both boards — 0/2 flips, headline totals unchanged. The runner predicate is fixed via an explicit row-mode manifest (POLICY 33, staged; the boards at this 2026-07-17 publish were still measured with the pre-manifest runner, so this disclosure remains in force for the `circt_verilog`/`Slang_circt` cells). Do not read these two page cells as full-pipeline lowering evidence, and do not read the four `bp_multicore*` greens as more than parse evidence. Note the `Arcilator` column never used `--parse-only`. At this publish FOUR of the six `black-parrot` `Arcilator` cells are GREEN on this page's opt-in arm — the two `bp_multicore_4*` rows are back at rc=71 against the budget (the register-retention price, the SoC bullet above) — and all six are green on the DEFAULT arm. CORRECTION OF RECORD (2026-08-23, standing): the prior revision of this bullet retained a stale "all six cells are RED" sentence from an earlier page even while the same page's SoC bullet listed them green — the sentence described the 2026-07-17 board and should have been retired at the 2026-08-15 publish; it is corrected here.

**Grading note.** `report.csv` carries the strict-graded record for the two CIRCT columns (fail-closed regrade of tool-reported successes). As of this publish the page's per-group cells and the CSV record **agree exactly for all four of our columns**: POLICY 32 keeps the emitted IR text out of the `circt_verilog` logs, so the stock generator's native log parse now matches the strict record (the previous pages' disclosed up-to-279-row understatement of `circt_verilog` on the page is gone), and at this publish the strict regrade changed ZERO rows on any column — the rendered record already carries the strict-graded verdicts (the single report-vs-strict divergence row is the renderer-lens row disclosed above). A publish gate now asserts page-cell == CSV for every tool x tag cell on both dashboards.
